#include<stdio.h>

int main()
{
    int a,b;
    printf("Enter the vlue of  a ");
    scanf("%d", &a);

    printf("Enter the value of b ");
    scanf("%d", &b);

    printf(" The sum of a and b id %d", a+ b);
    return 0;
}